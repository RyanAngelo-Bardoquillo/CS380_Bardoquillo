var age = 35;
var is_smoking = true;
var has_medical_condition = true;

if (age < 18) {
	print "Ineligible";
} else if (age > 60 or (is_smoking and has_medical_condition)) {
	print "high risk";
} else {
	print "low risk";
}
